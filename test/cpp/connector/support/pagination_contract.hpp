#pragma once

namespace cuac_test {

// Runs the closed pagination declaration, operation-binding, scoped-resource,
// and safe-explanation laws independently of the compiled package inventory.
void RunConnectorPaginationContractTests();

} // namespace cuac_test
